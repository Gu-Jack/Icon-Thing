const icons = document.querySelectorAll("i");
var find = Math.random * icons.length;

for(var i = 0; i < icons.length; i++) {
    icons[i].addEventListener("click", {
        if(i == find) {
                
        }
    })
}